# Project Guidelines

## Starting New Projects
When the user asks about starting a new project (e.g. Next.js), give practical advice about the best approach (e.g. `create-next-app` vs from scratch vs template). Do NOT just start coding immediately — first discuss the setup strategy with the user.
